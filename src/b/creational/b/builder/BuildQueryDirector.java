package b.creational.b.builder;

/**
 * 
 * The Director in builder design pattern.
 *
 */
public class BuildQueryDirector {
//	public Query buildQuery(QueryBuilder queryBuilder, String entityName, String where) {
//		queryBuilder.fromEntity(entityName);
//		queryBuilder.where(where);
//		return queryBuilder.getQuery();
//	}
}
